interface:
  display_name: "Local ASR (sherpa-onnx X-ASR)"
  short_description: "Transcribe audio/video locally with sherpa-onnx X-ASR (int8)."
  default_prompt: "Use $local-asr to transcribe a lecture video/audio locally with sherpa-onnx X-ASR and include --timestamps by default; omit it only when plain text is explicitly requested."
