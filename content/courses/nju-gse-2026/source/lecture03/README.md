# 第 3 讲网页阅读源材料

本目录归档《软件仓库管理》的 TeX、封面、配图、图注时间清单和视频元数据，用于双格式阅读构建。封面讲次已按原视频 `03-Raw/26生成式软件工程` 修正为第 3 讲。

发布版 PDF 未因网页化而修改。v1.0.1 起配图只保留课件区：原视频把黑板摄像头和课件拼在同一画面里，`layout.json` 在 1/15s 密集帧上测得课件区 `[545,0,1280,410]`，裁剪向内收 5 px，`figure_manifest.tsv` 的 `panel` 列记录每张图保留的区域。图 29 另附同一时刻的板书 `fig_26_gitv_visualizer_board.jpg`，对应“快照只复制到根的路径”一节的内容。课程根目录的 PDF 随之重新编译，现为 40 页。图片、TeX 和 PDF 的版本对应关系在课程 `course.json` 的 `web_source` 中固定。原始生成工作目录保留。
